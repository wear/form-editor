jest.dontMock('../RowComponent');

var React = require('react'),
    ReactDOM = require('react-dom');
    rowComponent = require('../RowComponent');
    TestUtils = require('react-addons-test-utils')

describe('row component', function() {
  it('render code', function() {
    var rowNode = TestUtils.renderIntoDocument(React.createElement("rowComponent", {lines: [{tag: 'code', body: 'code'}]}), 'p');
console.log(ReactDOM.findDOMNode(rowNode))

  })
});
