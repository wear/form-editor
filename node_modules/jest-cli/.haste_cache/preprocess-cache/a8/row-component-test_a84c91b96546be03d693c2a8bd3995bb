jest.dontMock('../RowComponent');

var React = require('react'),
    ReactDOM = require('react-dom');
    rowComponent = require('../RowComponent');
    TestUtils = require('react-addons-test-utils')


describe('row component', function() {
  it('render code', function() {
    var code = [{tag: 'code', body: 'code'}]
    var rowNode = TestUtils.renderIntoDocument(React.createElement("rowComponent", {lines: code}));
console.log(rowNode)
    // var text = TestUtils.findRenderedDOMComponentWithTag(row, 'p');
    // expect(ReactDOM.findDOMNode(text).textContent).toEqual('你的回答已提交成功！');
  })
});
